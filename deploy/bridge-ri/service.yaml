apiVersion: v1
kind: Service
metadata:
  name: bridge-ri
  namespace: orchestrator
  labels:
    app: bridge-ri
spec:
  type: ClusterIP
  selector:
    app: bridge-ri
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
